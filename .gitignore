testing/
release/
debug/
er-301

# Local-only operational notes that contain rig-specific identifiers
# (IP addresses, usernames, host paths). Kept on disk for personal
# reference; never tracked. The architectural context this draws on
# lives in stolmine repo's docs/planning/redesign/ series instead.
docs/dev-rig-procedures.md

# Python bytecode cache (reference harnesses)
__pycache__/
*.pyc

# Claude Code agent-local memory — session state, not repo content
.claude/memory/

# Stray local artifacts (druid REPL log, scratch archive)
planning/druid.log
planning/Archive.zip

# DSP harness stubs are generated by each tools/*-test/run.sh on every run.
tools/parametric-band-test/stub/
tools/parametric-eq-test/stub/
tools/pop3-dynamics-test/stub/
tools/stft-frontend-test/stub/
