- [Package trig LUT on am335x](feedback_package_trig_lut.md) — runtime sinf/cosf miscompute from package .so; use precomputed LUT for circular graphics.
- [Step-list count reduction clamp](feedback_steplist_count_clamp.md) — list-based graphics need C++ draw-time clamp AND Lua onCursorEnter reconcile to handle count drops cleanly.
- [Option vs Parameter for sub-controls](feedback_option_vs_parameter.md) — od::Option must use 1/2 values (never 0; it's CHOICE_UNKNOWN sentinel); for N>2 or CV use Parameter + ParameterAdapter + target/hardSet.
- [Serialize/deserialize pattern](feedback_serialize_deserialize_pattern.md) — adapter Bias round-trip template, C++-side enableSerialization, ConstantOffset/Comparator param names, and post-deserialize label refresh calls.
- [GainBias dual-mode focusedReadout](feedback_gainbias_dual_mode_focus.md) — shift-toggle subclasses must drive focusedReadout via the setFocusedReadout method (never direct assignment) in every code path; else heap corruption on teardown + onFocused nil-deref.
- [SWIG wrapper header dep gap](feedback_swig_header_dep.md) — editing a %include'd header doesn't retrigger SWIG; stale wrapper's sizeof corrupts the heap, crashing later on delete/quicksave. Force-clean the wrapper on header edits.
- [NEON 3-pass delay gather](feedback_neon_delay_gather.md) — split fused multitap loops into compute/gather/combine passes + 8-ahead prefetch + explicit NEON intrinsics on passes A/C. Got Pecto stereo from 50% to ~6% CPU. Template applies to Petrichor, Larets, Lofi delay.
- [Doppler baseDelay smoother for multitaps](feedback_doppler_basedelay_smoother.md) — single-pipeline LinearRamp slew on baseDelay scalar; SDK's two-read crossfade doubles NEON register pressure on multitaps and crashes Cortex-A8.
- [Multitap idx wrap ulp edge](feedback_multitap_idx_wrap_ulp.md) — `idx0 = (int)wrappedP` can return `maxDelay` (OOB) when float wrap rounds exactly. Always guard idx0 the same way you guard idx0+1. Surfaces only under continuous read-pointer slew (Doppler smoother).
- [Full-screen tile field effect](project_fullscreen_tile_field.md) — Colmatage BSP tile field at 128x64 looked striking; revisit for screensaver or full-width viz.
- [Preserve custom graphics in expansion](feedback_expansion_custom_graphic.md) — expansion layout must list the original control key first, not a duplicate fader, or the custom graphic is replaced by stock fader.
- [No third-party branding in unit descriptions](feedback_no_third_party_branding.md) — external-source unit ports get generic functional names (no Trinity/Modbap/BeatPPL/etc. in todo or planning).
- [Multi-output unit framework](project_multi_output_framework.md) — shipped in stolmine fork (QuadLFO ref impl); habitat author guide at docs/multi-output-units-author-guide.md. v1 contract: sub-out 1 primary, M6 cycles picker, args.channelCount+subOutLabels, vanilla-safe ABI.
- [Persist plans to repo before executing](feedback_persist_plans_to_repo.md) — after ExitPlanMode, fold the plan's substance into the repo's planning/ dir BEFORE the first code edit; ~/.claude/plans is ephemeral.
- [Spreadsheet paramMode UI convention](feedback_parammode_convention.md) — canonical shape for shift-toggle controls: paramMode flag, persistence across cursor leave, ShiftHelpers keyboard, grandfather init list, paramModeDefaultSub. Decisions 1–8 locked; D8 visible highlight PINNED.
- [Bump package version to force reinstall](feedback_package_version_bump.md) — ER-301 only re-extracts to rear SD when PKGVERSION changes; Lua-only builds with the same version silently run stale. Always bump.
- [subCursorController inheritance from GainBias.init](feedback_subcursor_inheritance.md) — GainBias.init writes self.bias into subCursorController; paramMode subclasses must redirect or clear on cursor enter. Complements the GainBias dual-mode focusedReadout memory.
- [Comparator gate threshold convention](feedback_comparator_gate_threshold.md) — when detecting rising edges from a Comparator-driven inlet, threshold the buffer with `> 0.5f` (not `> 0.0f`). 0.0 trips on uninitialized fuzz / DC residue and can hang the device when the rising-edge handler does substantial work.
- [Auto-install linux builds to emu](feedback_linux_build_auto_install.md) — every `make ARCH=linux` should be followed by `cp testing/linux/<pkg>.pkg ~/.od/rear/`. Don't ask, just install.
- [Identical means IDENTICAL](feedback_identical_means_identical.md) — when aping a reference pattern for hardware builds, "structurally identical" must be provable by line-by-line diff. Small divergences (if vs switch, shared vs fresh map, nested lambdas) can crash Cortex-A8 under -O3 -ffast-math while working fine in x86 emu.
- [Runtime-branched DSP dispatch can crash am335x](feedback_runtime_branched_dsp_dispatch.md) — switch / if-chain / fn-ptr table with differential bodies on a runtime tier value can hang Cortex-A8 even when each path is individually safe. Adding heavy work to ONE branch of a previously-OK switch can re-trigger the same crash. Pull per-sample work out of conditionals. Branchless arithmetic masking is the safe pattern.
- [NEON in DrumVoice — root cause isolated](feedback_neon_intrinsics_drumvoice.md) — NEON works in DrumVoice on Cortex-A8 IF arrays are class members (heap-allocated). Stack-local NEON arrays cause GCC `-O3 -ffast-math` to emit `vld1.32 [reg :64]` hints that trap on misalignment. Verify with `arm-none-eabi-objdump -d ... | grep vld1`.
- [NEON :64/:128 hints surface beyond stack-locals](feedback_neon_hint_surfaces.md) — register-pressure spills across function calls (e.g. simd_sin) and auto-vectorized init/ctor code can both emit trapping alignment hints. Run objdump on the WHOLE .o, not just process(). Construction-time crashes on insert have this signature.
- [Ngoma codex](project_ngoma_codex.md) — full architecture / audio path / modulator graph / NEON state / bisect history / sound-design knobs for the analog macro drum voice. Read before touching DrumVoice.cpp/.h.
- [Ngoma debug pipeline (active)](project_ngoma_debug_pipeline.md) — three-tier crash debug pipeline persisted to planning/ngoma-debug-pipeline.md; resume here after restart, await `.169` hardware result, build Tier 2 objdump pre-flight first.
- [Alembic codex](project_alembic_codex.md) — sample-trained 4-op PMM voice at 2.5.5.164. Signal chain, 49-float preset row, 3 independent scan positions, 8 plies, Phase 5a-5d-4 done. Read before AlembicVoice.cpp/.h or post-Phase-5 planning.
- [Encoder capture in viz is architectural](feedback_viz_encoder_capture_architectural.md) — UI lag from a graphic is almost always draw-path structure (tile granularity, state cache, time slicing), not CPU. Colmatage = reference; SomSphereGraphic = anti-pattern.
- [Standalone redesign docs and rpidev branch](reference_redesign_docs.md) — CM4/CM5 successor work lives on `er-301` submodule's `rpidev` branch + `er-301/docs/planning/redesign/` (19 files). Parallel-DSP MVP landed.
- [Handheld variant sketch](project_handheld_variant.md) — third variant idea: 4 outs + headphone, single stereo chain + globals, Pi Zero 2W, dpad-for-encoder. Not on v1 roadmap.
- [unitOutputNames table cap is fw-baked](feedback_unit_output_names_table.md) — multi-out units silently lose audio on slots beyond the table's cap; xroot is baked into emu/firmware so table extensions need fw rebuild. Stolmine post-2026-04-30: auto-generated Out1..Out99.
- [JF codex](project_jf_codex.md) — hex-voiced slope-engine voice in spreadsheet, clean-room from Mannequins tech map. Phase 1-5 done; Phase 6 polish remains. Read before JF.cpp/.h or jf/voice.h work.
- [Do not surrender rigor for expedience](feedback_no_lazy_paths.md) — when a crash bisects to a component, diagnose the mechanism, do not rip the component out. Stripping functionality to dodge a root cause leaves a landmine and ships a degraded unit. Stay rigorous.
- [No out-of-line virtuals on framework subclasses](feedback_no_out_of_line_virtuals.md) — applies to ANY package class subclassing an `od::*` framework type (od::Graphic, od::Object, etc., not just graphics). Out-of-line virtuals trigger GCC's key function rule, emit non-COMDAT vtables, hard-fault on insert across firmware rebuilds. DrumCubeGraphic + AlembicSphereGraphic had this trap. tools/check-graphic-virtual-defs.sh scans for it; docs/graphics-authoring-guide.md is the working-pattern reference.
